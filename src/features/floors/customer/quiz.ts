import { QuizDefinition } from '../../../config/quiz/types';

/**
 * Quizzes for the Customer Success room (floor 3, right side).
 *
 * Empty placeholder — ready for the Customer Success floor owner to add
 * content. Matches the convention used by the Product Leadership room.
 */
export const QUIZ_CUSTOMER: Record<string, QuizDefinition> = {};
